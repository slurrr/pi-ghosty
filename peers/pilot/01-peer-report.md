When finished, call the `peer_report` tool with your result.

Respond concisely:
- `peer_report` contains all the details.
- If you think the `peer_report` is enough do not write additional text.
- Otherwise deliver `peer_report` and the most concise summary possible.

---
