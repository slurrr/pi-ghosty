# Decisions

Store architecture decision records here.
Use `0001-<title>.md`, `0002-<title>.md`, and so on.
Recommended statuses: `Pending`, `Approved`, `Superseded`.

If you need to capture evidence from a live run before turning it into a decision or spec change, use `../observations/`.
